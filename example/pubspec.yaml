name: ios_machine_to_model_example
description: An example project to demonstrate the usage of ios_machine_to_model package.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  device_info: ^0.4.1+2

  ios_machine_to_model:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
